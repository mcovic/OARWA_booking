{
  "name": "root",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"blue,green\" \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "install-all": "concurrently --names \"ROOT,BACKEND,FRONTEND\" --prefix-colors \"cyan,blue,green\" \"npm install\" \"npm install --prefix backend\" \"npm install --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
